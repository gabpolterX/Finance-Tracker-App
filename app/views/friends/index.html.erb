<h1>My Friends  <%= fa_icon 'users' %></h1>

<div class="search-area">
    <h3>Search Friends</h3>
    <%= form_with url: friends_path, method: :get, data: { turbo: false}, remote: true do %>
        <div class="form-group row">
            <div class="col-sm-9" no-right-padding>
                <%= text_field_tag :friend, params[:friend], placeholder: "Name or email", autofocus: true, class: "form-control form-control-lg" %>
            </div>
            <div class="col-sm-3 no-left-padding ">
                <%= button_tag type: :submit, class: "btn btn-success" do %>
                    <%= fa_icon 'search 2x' %>
                <% end %>
            </div>
        </div>
    <% end %>
</div>

<%= render 'list' %>